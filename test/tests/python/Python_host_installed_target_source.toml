# Build a host Python from an installed version, and a target Python from the
# same source version.

[Python]
version = "$PYTHON_VERSION"
install_host_from_source = false


[Qt]
version = "$QT_VERSION"
install_from_source = false
